# Django Settings
DJANGO_SECRET_KEY=your-secret-key-change-this-in-production-make-it-long-and-random
DJANGO_SETTINGS_MODULE=config.settings.development

# Database (Neon.tech PostgreSQL)
DB_NAME=vaultshare
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=your-neon-host.neon.tech
DB_PORT=5432
DB_SSL_MODE=require

# Storage Configuration - Firebase (FREE!)
# Development uses local files, production uses Firebase
STORAGE_BACKEND=local

# Firebase Storage Setup (for production):
# 1. Go to https://console.firebase.google.com/
# 2. Create project → Enable Storage
# 3. Download service account JSON from Project Settings
# 4. Set path below or use base64 in FIREBASE_CREDENTIALS_JSON
FIREBASE_CREDENTIALS_PATH=/path/to/firebase-credentials.json
FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Redis/Upstash Configuration
REDIS_URL=redis://localhost:6379/0
# For Upstash: REDIS_URL=rediss://default:your_password@your-endpoint.upstash.io:6379

# Email - Brevo (FREE 300 emails/day)
# Sign up: https://www.brevo.com/ → Get API key from Settings → API Keys
BREVO_API_KEY=your_brevo_api_key_here
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# Sentry (Optional)
SENTRY_DSN=your_sentry_dsn_here

# Application Settings
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Production-only settings (leave empty for development)
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# CORS_ALLOWED_ORIGINS=https://yourdomain.com
# FRONTEND_URL=https://yourdomain.com
